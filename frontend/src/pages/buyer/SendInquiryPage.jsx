import Screen from "../../components/common/Screen";

const SendInquiryPage = () => {
  return <Screen title="Send an inquiry" description="Ask the property owner for availability and a viewing." />;
};

export default SendInquiryPage;
