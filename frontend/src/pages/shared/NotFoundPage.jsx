import Screen from "../../components/common/Screen";

const NotFoundPage = () => {
  return <Screen title="Page not found" description="The page you requested does not exist or has moved." />;
};

export default NotFoundPage;
